{
  "name": "brunos-alarm-functions",
  "description": "Cloud Functions for Bruno's Alarm live chat — moderation + rate limiting live server-side here, not trusted to the client.",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "deploy": "npm run build && firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@google-cloud/firestore": "^9.2.0",
    "bad-words": "^4.1.5",
    "firebase-admin": "^14.4.0",
    "firebase-functions": "^7.4.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "private": true
}
